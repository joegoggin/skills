interface:
  display_name: "Documentation"
  short_description: "Apply project documentation conventions"
  default_prompt: "Use $docs to update missing, stale, misleading, or convention-mismatched documentation in current-branch and untracked workspace changes."
